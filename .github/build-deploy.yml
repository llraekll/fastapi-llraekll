name: Build and Deploy code
on: [push, pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/chechout@v2
      - name: say hi to human
        run: echo "hello human"